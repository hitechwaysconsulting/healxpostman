$kind: http-request
name: Get Profile
url: "{{base_url}}/user/profile"
method: GET
headers:
  Authorization: Bearer {{token}}
  Content-Type: application/json
auth:
  type: bearer
  bearer:
    token: jwt123
order: 3100
